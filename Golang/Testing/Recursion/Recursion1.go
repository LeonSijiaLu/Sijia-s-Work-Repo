package main

import (
	"fmt"
//	"os"
//	"bufio"
)

func main(){
	//reader := bufio.NewReader(os.Stdin)
	//input,_ := reader.ReadString('\n')
	a := 0
	if a == 0{
		fmt.Println(a)
		a ++
		fmt.Println(a)
	}else if a == 1{
		fmt.Println(a)
	}
}